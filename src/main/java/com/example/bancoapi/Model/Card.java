package com.example.bancoapi.Model;

public record Card(Long id, String cardNumber, String cardType) {
    // - Construtor
    // - Getters (id(), cardNumber(), cardType())
    // - equals(), hashCode(), toString()
}